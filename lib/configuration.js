



module.exports = {

  // automatically expand compund keys, those containing multiple nodes
  // separated by a period, pound, or a forward slash. If option is
  // false, all keys values are interpreted literally and not expanded.
  expandCompoundKeys: true,

  // if set to true, enforces a single-callback-per-action limitation
  // rather than allowing for multiple callbacks per action.
  singleCallbackPerAction: false,

};
